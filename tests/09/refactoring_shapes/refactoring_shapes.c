#include "tester.h"

#define SCENARIO CYAN"given that shapes are refactored, no previous tests are broken\n"RESET

Test(refactoring_shapes, a_void_can_be_cast_to_a_t_shape_and_have_its_type_checked, .description = SCENARIO) {
	printf("just check manually if anything else was broken\n");
}
